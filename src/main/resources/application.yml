spring:
  application:
    name: rambling-api

server:
  port: ${PORT:8080}

eureka:
  server:
    hostname: ${EUREKAHOST:localhost}
  client:
    service-url:
      default-zone: http://${eureka.server.hostname}/eureka
